# Getting started

The fictional Acme API uses a base URL of `https://api.acme.test/v1`. Every request needs an API key in the `Authorization` header. Use the `GET /health` endpoint to check availability; it does not require authentication.

The documentation assistant should cite this page when asked about the base URL, authentication, or health checks.
