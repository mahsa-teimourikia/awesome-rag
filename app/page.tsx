"use client";

import { useEffect, useMemo, useState } from "react";

type Level = "Beginner" | "Intermediate" | "Advanced";
type Subject = { id:string; level:Level; step:string; title:string; description:string; time:string; outcome:string; lesson:string; exercise:string; failures:string[]; notebook:string; refs:string[]; code:string; quiz:{q:string; options:string[]; answer:number}[] };

const base = "https://github.com/mahsa-teimourikia/awesome-rag/blob/main/";
const guidePaths:Record<string,string>={
 b1:"curriculum/beginner/01-rag-foundations/README.md",b2:"curriculum/beginner/02-first-local-rag/README.md",b3:"curriculum/beginner/03-chunking-lab/README.md",b4:"curriculum/beginner/04-citations-abstention/README.md",b5:"curriculum/beginner/05-capstone-enterprise-rag/README.md",
 i1:"curriculum/intermediate/01-retrieval-strategies/README.md",i2:"curriculum/intermediate/02-metadata-permissions/README.md",i3:"curriculum/intermediate/03-query-reranking/README.md",i4:"curriculum/intermediate/04-evaluation/README.md",i5:"curriculum/intermediate/05-research-synthesis/README.md",i6:"curriculum/intermediate/06-qdrant-local/README.md",
 a1:"curriculum/advanced/01-corrective-rag/README.md",a2:"curriculum/advanced/02-graphrag/README.md",a3:"curriculum/advanced/03-agentic-rag/README.md",a4:"curriculum/advanced/04-structured-multimodal/README.md",a5:"curriculum/advanced/05-adaptive-rag/README.md",a6:"curriculum/advanced/06-production-operations/README.md"
};

const subjects:Subject[] = [
 {id:"b1",level:"Beginner",step:"01",title:"RAG foundations",description:"Understand the pipeline, parametric knowledge, retrieved evidence, grounding, and abstention.",time:"20 min",outcome:"Explain the difference between a model’s parametric knowledge and retrieved evidence; name ingestion, chunking, indexing, retrieval, generation, and evaluation.",lesson:"RAG connects a language model to external knowledge at answer time. The corpus acts as non-parametric memory: retrieve relevant evidence, then generate a response grounded in that evidence. The two quality questions are separate: did we retrieve the right evidence, and did the model answer faithfully from it?",exercise:"Draw the pipeline from memory and label one failure mode for each stage. Compare it with the architecture diagram in the repo.",failures:["Missing or broken extraction", "Chunks that split the answer", "Weak retrieval admitted as context", "Fluent but unsupported generation"],notebook:"curriculum/beginner/01-rag-foundations/rag_foundations.ipynb",refs:["curriculum/beginner/01-rag-foundations/README.md"],code:"documents → chunks → index\nquestion → retrieve evidence\nevidence → grounded answer\nanswer → evaluate + cite",quiz:[{q:"What is the main advantage of retrieval-augmented generation?",options:["It grounds answers in external, up-to-date evidence.","It requires retraining the model daily.","It guarantees that no hallucinations occur."],answer:0}]},
 {id:"b2",level:"Beginner",step:"02",title:"First local baseline",description:"Build an inspectable Harborline Support baseline: trace lexical matches, bound context, and evaluate abstention.",time:"75–90 min",outcome:"Load local Markdown, inspect ranked terms and source IDs, build a bounded context window, and measure retrieval plus abstention on a golden set.",lesson:"Harborline Support begins with transparent lexical retrieval so every score, match, source ID, and no-answer decision is visible. The notebook separates retrieval quality from answer faithfulness before adding embeddings or a model API.",exercise:"Run the Harborline golden set. Change top_k or min_score one at a time, compare retrieval hits with abstention correctness, then investigate the deliberate synonym failure.",failures:["Lexical overlap misses synonyms", "Paragraphs are too large or small", "High thresholds create false abstentions", "Low thresholds admit weak evidence"],notebook:"curriculum/beginner/02-first-local-rag/02_first_local_rag.ipynb",refs:["curriculum/beginner/02-first-local-rag/README.md"],code:"chunks = load_markdown_documents()\ncandidates = retrieve(question, chunks, top_k=3)\nif top_score < min_score:\n    return abstain(\"not enough evidence\")",quiz:[{q:"What is the primary benefit of a deterministic lexical baseline?",options:["It makes retrieval failures (like synonym mismatch) fully inspectable.","Lexical search is always more accurate than semantic search.","It eliminates the need for an LLM."],answer:0}]},
 {id:"b3",level:"Beginner",step:"03",title:"Chunking lab",description:"Use Harborline policy documents to compare fixed, heading-aware, and sentence-window chunks against evidence coverage.",time:"80–100 min",outcome:"Choose and justify a chunking strategy by measuring chunk diagnostics, metadata preservation, and whether evidence stays intact for real questions.",lesson:"Chunking is an information-design decision. The Harborline lab compares predictable fixed windows, document headings, and sentence windows, then exposes how a boundary can separate an action from its required approval.",exercise:"Compare all three strategies on Harborline’s support handbook. Measure chunk diagnostics and test whether restart plus approval survive in one evidence unit.",failures:["Definitions separated from qualifications", "Sections larger than the context window", "Overlap duplicated and costly text", "Tables or code flattened into noise"],notebook:"curriculum/beginner/03-chunking-lab/03_chunking_lab.ipynb",refs:["curriculum/beginner/03-chunking-lab/README.md"],code:"fixed = fixed_size(document, size=500, overlap=50)\nstructured = by_heading(document)\ncompare(fixed, structured, questions)",quiz:[{q:"Which chunking practice generally improves retrieval quality?",options:["Splitting on document structure (like headings).","Using the largest possible chunk for every corpus.","Always splitting by exact token counts regardless of sentences."],answer:0}]},
 {id:"b4",level:"Beginner",step:"04",title:"Citations & abstention",description:"Audit structured Harborline evidence, distinguish weak from ambiguous retrieval, and make abstention policy explicit.",time:"80–100 min",outcome:"Return auditable evidence objects, validate provenance before rendering, and explain why a request was answered or abstained.",lesson:"A citation is data, not formatting. The notebook distinguishes insufficient evidence, ambiguous evidence, and insufficient source diversity while showing that a valid source ID alone does not prove a claim is supported.",exercise:"Compare threshold and margin policies on answerable, unsupported, and high-risk questions. Audit each result before rendering and design a claim-level response contract.",failures:["A real document may not support the claim", "Retrieved content may be unauthorized", "Thresholds tuned on easy examples", "Citation strings formatted too early"],notebook:"curriculum/beginner/04-citations-abstention/04_citations_abstention.ipynb",refs:["curriculum/beginner/04-citations-abstention/README.md"],code:"candidates = retrieve(question)\nif not candidates or candidates[0].score < threshold:\n    return CitedAnswer.abstain(reason=\"weak evidence\")\nreturn answer_with_citations(candidates)",quiz:[{q:"Why is abstention a safer generation behavior?",options:["It avoids inventing support when evidence is missing.","It always improves recall.","It removes the need for evaluation."],answer:0}]},
 {id:"b5",level:"Beginner",step:"05",title:"Enterprise RAG capstone",description:"Build an end-to-end Enterprise RAG system using a real vector store, embeddings, LLM, and evaluation dataset.",time:"120–180 min",outcome:"Integrate ingestion, structure-aware chunking, vector indexing, structured generation, and deterministic validation into a cohesive pipeline.",lesson:"Enterprise RAG is not simply calling a single chain. A robust system requires an ingestion pipeline that respects document structure, a real vector store, explicit orchestration, and quantitative evaluation.",exercise:"Execute the capstone notebook to build a local index of synthetic enterprise documents, then observe how the LLM handles multi-source, unanswerable, and conflicting-evidence queries.",failures:["Hiding the pipeline in an opaque chain", "Treating retrieval failures as generation failures", "Using mock embeddings in production", "Assuming all documents are conflict-free"],notebook:"curriculum/beginner/05-capstone-enterprise-rag/05_enterprise_rag_capstone.ipynb",refs:["curriculum/beginner/05-capstone-enterprise-rag/README.md"],code:"candidates = retrieve_candidates(query)\ncontext = build_evidence_context(candidates)\ndecision = generate_decision(query, context)\nvalidate_decision(decision)",quiz:[{q:"Why is it important to separate retrieval evaluation from generation evaluation?",options:["Because a generation failure cannot fix a retrieval failure where evidence was never supplied.","Because it makes the system run faster.","Because LLMs cannot read retrieved evidence."],answer:0}]},
 {id:"i1",level:"Intermediate",step:"01",title:"BM25, dense & hybrid",description:"Compare exact-term and semantic retrieval, then fuse independent rankings with reciprocal-rank fusion.",time:"45 min",outcome:"Understand why BM25 remains useful for identifiers and combine lexical and dense signals for better recall.",lesson:"Lexical retrieval is strong for names, error codes, and exact phrases. Dense retrieval matches paraphrases. Hybrid retrieval treats both as useful signals, and reciprocal-rank fusion combines positions without assuming the raw scores are comparable.",exercise:"Add a paraphrase query and an exact error-code query. Explain which ranking is stronger for each before choosing weights or top_k.",failures:["Dense search misses exact identifiers", "Lexical search misses paraphrases", "Raw scores from different systems are incomparable", "Hybrid weights tuned without a golden set"],notebook:"curriculum/intermediate/01-retrieval-strategies/01_retrieval_strategies.ipynb",refs:["curriculum/intermediate/01-retrieval-strategies/README.md"],code:"lexical = bm25.search(query)\ndense = dense_adapter.search(query)\nhybrid = reciprocal_rank_fusion(lexical, dense)",quiz:[{q:"When does lexical retrieval often outperform dense retrieval?",options:["On queries containing exact error codes or identifiers.","On queries requiring loose paraphrases.","On conversational, vague queries."],answer:0}]},
 {id:"i2",level:"Intermediate",step:"02",title:"Metadata permissions",description:"Enforce tenant and tag authorization before retrieval so unsafe documents never enter context.",time:"40 min",outcome:"Apply authorization at the document query boundary and test the boundary directly.",lesson:"Filtering after retrieval is too late: an unauthorized chunk may already influence ranking, prompt construction, logs, caches, or an intermediate model call. The policy filter must produce an authorized subset before the retriever sees it.",exercise:"Add a role requirement or document expiration timestamp. Prove that a cross-tenant document cannot be retrieved even when its text exactly matches the query.",failures:["Relying on the model to hide text", "Filtering after context construction", "Missing tenant metadata", "No permission-boundary test"],notebook:"curriculum/intermediate/02-metadata-permissions/02_metadata_permissions.ipynb",refs:["curriculum/intermediate/02-metadata-permissions/README.md"],code:"authorized = policy_filter(index,\n  user.tenant_id, user.roles)\ncontext = retriever.search(query,\n  corpus=authorized)",quiz:[{q:"When must document authorization happen in a RAG system?",options:["Before retrieval (e.g. as a filter on the index).","After generation (by filtering the output).","Only in the UI layer."],answer:0}]},
 {id:"i3",level:"Intermediate",step:"03",title:"Rewriting & reranking",description:"Improve difficult queries by retrieving from focused variants, then applying a second-stage ranker.",time:"45 min",outcome:"Improve recall with query variants and improve ordering with a bounded, stronger reranker.",lesson:"Rewriting can improve recall but may introduce irrelevant candidates and cost. Reranking is more expensive than first-stage retrieval, so apply it to a bounded candidate set and validate input length, latency, and score calibration.",exercise:"Add a paraphrase the original query misses. Compare recall before and after rewriting, then measure the first relevant result’s position after reranking.",failures:["Rewrites drift from user intent", "Candidate sets become noisy", "Reranking the whole corpus is too expensive", "Uncalibrated scores look authoritative"],notebook:"curriculum/intermediate/03-query-reranking/03_query_reranking.ipynb",refs:["curriculum/intermediate/03-query-reranking/README.md"],code:"variants = rewrite_and_expand(query)\ncandidates = union(retrieve(v) for v in variants)\ntop_k = reranker.rank(query, candidates)[:5]",quiz:[{q:"What is the purpose of a second-stage reranker?",options:["To apply a more accurate, expensive relevance model to a bounded candidate set.","To rewrite the user's query into multiple sub-queries.","To retrieve candidates as fast as possible."],answer:0}]},
 {id:"i4",level:"Intermediate",step:"04",title:"Evaluation lab",description:"Create a golden set, calculate retrieval metrics, and gate regressions before shipping changes.",time:"45 min",outcome:"Measure recall@k, precision@k, and MRR, then pair retrieval metrics with groundedness, latency, cost, and security tests.",lesson:"A golden dataset turns RAG iteration into an engineering discipline. Retrieval metrics answer whether labeled relevant evidence was found; they do not prove answer faithfulness. Inspect changed failures rather than averages alone.",exercise:"Add a no-answer query, a permission-boundary query, and a paraphrase. Set minimum recall and MRR thresholds, then make a deliberately bad ranking fail the gate.",failures:["Testing only happy-path demos", "Blaming generation for retrieval misses", "LLM judges without calibration", "No regression threshold"],notebook:"curriculum/intermediate/04-evaluation/01_building_eval_datasets.ipynb",refs:["curriculum/intermediate/04-evaluation/README.md"],code:"for case in golden_set:\n  ranked = retriever(case.question)\n  scores = metrics(ranked, case.relevant_ids)\nassert report.recall_at_k >= gate",quiz:[{q:"Which metric is used to evaluate the retrieval stage?",options:["Recall@k","Writing-style preference","Answer faithfulness"],answer:0}]},
 {id:"i5",level:"Intermediate",step:"05",title:"Research synthesis",description:"Use multiple focused queries, deduplicate evidence, and preserve claim-level citations.",time:"60 min",outcome:"Build research-style answers that separate findings, limitations, and open questions.",lesson:"One query often overfits to its first framing. Retrieve evidence, findings, limitations, and counterarguments separately. Deduplication prevents repeated copies of the same source from dominating context, while claim-level citations preserve provenance.",exercise:"Add two documents supporting a claim and one limitation. Extend each claim with confidence and a type such as finding, limitation, or open-question.",failures:["Single-query framing bias", "Duplicate sources dominate context", "Claims lose source links during synthesis", "Counterevidence is omitted"],notebook:"curriculum/intermediate/05-research-synthesis/05_research_synthesis.ipynb",refs:["curriculum/intermediate/05-research-synthesis/README.md"],code:"queries = focus(question, [\"findings\", \"limits\", \"counterpoints\"])\nevidence = deduplicate(flatten(retrieve(q) for q in queries))\nreturn synthesize_claims(evidence)",quiz:[{q:"Why use multiple focused queries for research synthesis?",options:["To prevent framing bias and retrieve diverse evidence.","To intentionally duplicate context.","To reduce the number of API calls."],answer:0}]},
 {id:"i6",level:"Intermediate",step:"06",title:"Local Qdrant & embeddings",description:"Replace the baseline with Sentence Transformers and Qdrant while keeping payload metadata and tenant filters.",time:"60 min",outcome:"Run a local vector search stack without losing the conceptual and dependency-free baseline.",lesson:"The optional infrastructure lab introduces embeddings and a vector store after the retrieval concepts are clear. Qdrant provides vector search, payload metadata, and filters; the baseline remains valuable because it keeps behavior inspectable and runnable without Docker or model downloads.",exercise:"Start Qdrant locally, index the beginner documents, and compare dense retrieval with the dependency-free baseline on the same questions.",failures:["Treating vector search as a quality guarantee", "Dropping payload metadata", "No tenant filters", "Benchmarking only one query"],notebook:"curriculum/intermediate/06-qdrant-local/06_qdrant_local.ipynb",refs:["curriculum/intermediate/06-qdrant-local/README.md"],code:"vectors = embed(chunks)\nclient.upsert(collection, vectors, payload=metadata)\nresults = client.search(query_vector,\n  query_filter=tenant_filter)",quiz:[{q:"When using a vector database, what metadata must you retain?",options:["Source document provenance and tenant permissions.","Only the embedding vector.","The user's secret keys."],answer:0}]},
 {id:"a1",level:"Advanced",step:"01",title:"Corrective RAG",description:"Detect weak retrieval, choose a bounded recovery route, and abstain when recovery still lacks evidence.",time:"60 min",outcome:"Record route, confidence, latency, and final outcome for every corrective attempt.",lesson:"Corrective RAG is not ‘keep trying until something matches.’ Grade the first retrieval, reformulate or choose an alternate retriever when confidence is low, preserve the trace, bound retries, and make abstention a valid terminal state.",exercise:"Add a maximum retry budget and a second retriever. Test a question that should abstain after reformulation.",failures:["Unbounded retry loops", "Unauthorized fallback sources", "Recovery without a trace", "Forcing an answer after all routes fail"],notebook:"curriculum/advanced/01-corrective-rag/01_corrective_rag.ipynb",refs:["curriculum/advanced/01-corrective-rag/README.md"],code:"docs = retrieve(question)\nif not grade(docs):\n  docs = alternate(rewrite(question))\nreturn accept_or_abstain(docs, trace=route_log)",quiz:[{q:"What is a valid terminal state in a Corrective RAG loop?",options:["Abstaining when recovery routes fail to find evidence.","Retrying infinitely until a document matches.","Hallucinating an answer if the retry budget runs out."],answer:0}]},
 {id:"a2",level:"Advanced",step:"02",title:"GraphRAG & entity retrieval",description:"Represent relationships explicitly and retrieve bounded graph neighborhoods with fact-level provenance.",time:"60 min",outcome:"Answer relationship-heavy and corpus-level questions without allowing traversal to cross authorization boundaries.",lesson:"Graph retrieval complements text retrieval. It is useful for relationships, paths, and corpus-level questions; isolated factual passages may still be better served by lexical or dense search. Bound traversal depth and apply the same authorization filters to nodes and edges.",exercise:"Add a two-hop question, a missing-entity question, and tenant metadata to facts. Test that traversal cannot cross a tenant boundary.",failures:["Unbounded graph traversal", "Graph used for every question", "Fact provenance lost", "Unauthorized nodes reachable"],notebook:"curriculum/advanced/02-graphrag/02_graphrag.ipynb",refs:["curriculum/advanced/02-graphrag/README.md"],code:"entities, edges = extract_graph(documents)\nneighborhood = traverse(entity, depth=2,\n  filter=authorized_tenant)\nreturn facts_with_sources(neighborhood)",quiz:[{q:"When is GraphRAG especially useful?",options:["For answering corpus-level aggregation questions and paths.","For every simple isolated definition.","For bypassing tenant authorization bounds."],answer:0}]},
 {id:"a3",level:"Advanced",step:"03",title:"Agentic RAG & tool boundaries",description:"Route questions between retrieval, tools, and human escalation with explicit approval and trace records.",time:"60 min",outcome:"Keep identity, allowlists, argument schemas, budgets, approval, idempotency, and verification outside the model.",lesson:"An agent is not authorized merely because a model selected a tool. The planner can route to knowledge, an action, or escalation, but the system enforces permissions and approval, validates tool arguments, verifies receipts, and records the route.",exercise:"Add a read-only tool and a destructive tool. Require approval for the destructive one and test that denial cannot execute a side effect.",failures:["Model-selected tools bypass policy", "Destructive actions without approval", "No idempotency or receipt", "Ambiguous requests silently routed"],notebook:"curriculum/advanced/03-agentic-rag/03_agentic_rag.ipynb",refs:["curriculum/advanced/03-agentic-rag/README.md"],code:"route = planner(question)\nif route.action and needs_approval(route):\n  require_human_approval()\nreceipt = execute_allowlisted_tool(route)\nverify(receipt)",quiz:[{q:"Who is responsible for enforcing tool authorization in Agentic RAG?",options:["The application boundary.","The language model reasoning loop.","The agent's system prompt."],answer:0}]},
 {id:"a4",level:"Advanced",step:"04",title:"Structured & multimodal RAG",description:"Route table questions to typed operations and treat images, audio, OCR, and text as modality-specific evidence.",time:"60 min",outcome:"Preserve row-level provenance, validate schemas and units, and avoid forcing aggregation through text similarity.",lesson:"A table question should use typed filtering or SQL; an image may need OCR and layout provenance; text can use ordinary retrieval. The evidence layer should preserve row IDs, source locations, units, and permissions across modalities.",exercise:"Add a currency or date field and validate it before aggregation. Add an OCR image asset and compare its citation with a plain text source.",failures:["Numeric aggregation from similarity alone", "Unit and schema errors", "OCR provenance discarded", "Permissions applied only to text"],notebook:"curriculum/advanced/04-structured-multimodal/04_structured_multimodal.ipynb",refs:["curriculum/advanced/04-structured-multimodal/README.md"],code:"kind = classify(question)\nif kind == \"table\": evidence = sql(query, schema)\nelif kind == \"image\": evidence = ocr_with_layout(asset)\nelse: evidence = text_retrieve(question)",quiz:[{q:"How should you answer an exact numeric aggregation question?",options:["Using typed tools or SQL rather than text similarity.","By generating multiple query formulations.","By retrieving a larger chunk of text."],answer:0}]},
 {id:"a5",level:"Advanced",step:"05",title:"Adaptive RAG",description:"Route each query to the minimum retrieval strategy — none, single-step, or iterative — based on complexity, freshness need, and cost budget.",time:"60 min",outcome:"Build a query router that classifies intent and routes to the appropriate path: direct answer, single-step dense retrieval, or an iterative agentic loop with bounded retries.",lesson:"Standard RAG sends every request through the same pipeline regardless of difficulty. Adaptive RAG inserts a router before retrieval: analyze query complexity, freshness requirements, and risk, then choose the minimum retrieval and reasoning strategy that reliably answers it. Bypassing retrieval for simple queries reduces latency and cost; routing hard queries to iterative retrieval improves quality. Linguistic complexity is not retrieval complexity — a short question about a proprietary system may need full governed retrieval while a long general question may need none.",exercise:"Add a query that needs web search, one that can be answered directly from parametric knowledge, and one that requires internal document search. Verify each routes correctly. Then set a cost budget for the iterative path and test that the router respects it.",failures:["Routing on lexical complexity rather than retrieval need","No cost or latency budget on the iterative path","Mixing routing logic with retrieval and authorization logic","Treating routing errors as silent fallbacks"],notebook:"curriculum/advanced/05-adaptive-rag/06_adaptive_rag.ipynb",refs:["curriculum/advanced/05-adaptive-rag/README.md","curriculum/advanced/05-adaptive-rag/06_adaptive_rag.ipynb"],code:"intent = classify(query)  # direct / single / iterative\nif intent == \"direct\":\n    return answer_from_params(query)\nelif intent == \"single\":\n    return rag_single_step(query)\nelse:\n    return rag_iterative(query, budget=retry_budget)",quiz:[{q:"What does an Adaptive RAG router decide before retrieval starts?",options:["Whether and how to retrieve, based on query complexity and freshness need.","Whether the user is authorized to access the system.","Which chunk size to apply to the full corpus."],answer:0},{q:"Why might routing a simple query to a direct answer improve the system?",options:["It avoids retrieval latency and cost when external evidence is not needed.","It always produces more accurate answers than retrieval.","It bypasses authorization checks for faster responses."],answer:0}]},
 {id:"a6",level:"Advanced",step:"06",title:"Production operations",description:"Instrument requests, enforce SLO budgets, detect stale indexes, gate releases on evaluation metrics, and keep a safe rollback path.",time:"60–90 min",outcome:"Build an operational contract: trace routes and retrieval, set freshness and latency SLOs, gate releases on a golden-set threshold, observe canary traffic before full promotion, and roll back safely.",lesson:"Operational readiness is more than a successful model call. A response can be fluent and grounded while the system is unsafe: the index is stale, a retrieval dependency is timing out, an evaluator is failing open, or a trace leaks sensitive text. Trace ingestion, retrieval, generation, and tools; version prompts, chunking, and embedding models together; set freshness, latency, cost, and quality SLOs; minimize sensitive log content; gate releases on offline evaluation; observe canary traffic before full promotion; and keep a rollback or kill switch ready.",exercise:"Add a trace ID, circuit-breaker state, and freshness threshold per tenant. Make the system refuse deployment when a golden-set metric or readiness check fails. Simulate a stale index and verify the fallback triggers correctly.",failures:["Stale indexes going unnoticed until user complaints arrive","No per-route cost or latency budget","Sensitive content leaking into logs or traces","Deploying without offline evaluation gates or a rollback plan"],notebook:"curriculum/advanced/06-production-operations/05_production_operations.ipynb",refs:["curriculum/advanced/06-production-operations/README.md","curriculum/advanced/06-production-operations/05_production_operations.ipynb"],code:"trace = start_trace(request)\nwith budget(latency_ms=1500, cost=0.02):\n  response = run_rag(request, trace)\nreadiness = freshness_ok() and eval_health()\nreturn response if readiness else fallback()",quiz:[{q:"Which controls make a production RAG rollout safer?",options:["Gating on evaluation metrics, tracing, and a gradual canary rollout.","Disabling regression evaluation to ship faster.","Deleting old indexes immediately after the new version is promoted."],answer:0},{q:"What should you NOT log by default in a production RAG trace?",options:["Raw prompts, private document content, secrets, and full tool output.","Request IDs, latency by stage, and model versions.","Retrieval candidate IDs and evaluator results."],answer:0}]}
];

const levels:Record<Level,{sub:string;color:string}>={Beginner:{sub:"Build one trustworthy assistant",color:"mint"},Intermediate:{sub:"Improve and measure retrieval",color:"gold"},Advanced:{sub:"Design for recovery and operations",color:"coral"}};

export default function Home(){
 const [level,setLevel]=useState<"All"|Level>("All"); const [selected,setSelected]=useState(subjects[0]); const [tab,setTab]=useState<"learn"|"lab"|"quiz">("learn"); const [answers,setAnswers]=useState<Record<number,number>>({}); const [completed,setCompleted]=useState<string[]>([]);
 useEffect(()=>{try{const saved=window.localStorage.getItem("rag-field-guide-progress-v1");if(saved)setCompleted(JSON.parse(saved));}catch{}} ,[]);
 useEffect(()=>{try{window.localStorage.setItem("rag-field-guide-progress-v1",JSON.stringify(completed));}catch{}} ,[completed]);
 const filtered=useMemo(()=>level==="All"?subjects:subjects.filter(s=>s.level===level),[level]); const score=selected.quiz.reduce((n,q,i)=>n+(answers[i]===q.answer?1:0),0);
 const select=(s:Subject)=>{setSelected(s);setTab("learn");setAnswers({});window.setTimeout(()=>document.getElementById("lesson-workspace")?.scrollIntoView({behavior:"smooth",block:"start"}),0);};
 return <main>
  <nav className="nav"><div className="brand"><span className="brand-mark">✦</span><span>RAG / <em>FIELD GUIDE</em></span></div><div className="nav-links"><a href="#curriculum">Curriculum</a><a href="/awesome-rag/quiz/">Full quiz</a><a href="#how">How it works</a><a className="repo" href="https://github.com/mahsa-teimourikia/awesome-rag" target="_blank">Open repo ↗</a></div></nav>
  <section className="hero"><div className="hero-copy"><div className="eyebrow">THE OPEN-SOURCE PATH · FROM THEORY TO OPERATIONS</div><h1>Build answers<br/><span>that know why.</span></h1><p className="hero-lede">A notebook-first learning hub built from the <em>awesome-rag</em> repository: follow one scenario, learn the theory, run the Python, break the system, measure the fix, and keep the evidence.</p><div className="hero-actions"><a className="button primary" href="#curriculum">Start the curriculum <span>↓</span></a><a className="text-link" href="/awesome-rag/quiz/">Take the full quiz ↗</a></div><div className="hero-meta"><span><b>16</b> scenario notebooks</span><span><b>16</b> repo lessons</span><span><b>1</b> learning hub</span></div></div><div className="hero-art"><div className="orbit orbit-one"/><div className="orbit orbit-two"/><div className="core"><span>RAG</span><small>grounded generation</small></div><div className="node node-a">01<br/><b>RETRIEVE</b></div><div className="node node-b">02<br/><b>GENERATE</b></div><div className="node node-c">03<br/><b>VERIFY</b></div><div className="art-caption">Your model is only<br/><strong>as good as its context.</strong></div></div></section>
  <section className="signal"><div><span className="signal-icon">↗</span><b>FOLLOW THE REPO</b><p>Objectives, notebooks, exercises, and next steps are mapped from the source material.</p></div><div><span className="signal-icon">⌁</span><b>LEARN IN LOOPS</b><p>Read a concept. Run the lab. Change one variable. Inspect the failure.</p></div><div><span className="signal-icon">◌</span><b>CHECK YOUR SIGNAL</b><p>Use the checkpoint quiz and the repo’s tests to see what actually stuck.</p></div></section>

  <section id="curriculum" className="curriculum"><div className="section-intro"><div><div className="eyebrow">THE CURRICULUM MAP</div><h2>One path.<br/>Three altitudes.</h2></div><p>The repo recommends starting with a beginner assistant, then improving retrieval with measurable experiments, and finally designing for recovery, security, multimodality, and operations.</p></div><div className="progress-summary" aria-live="polite">{completed.length} of {subjects.length} lessons complete</div><div className="level-tabs"><button className={level==="All"?"active":""} onClick={()=>setLevel("All")}>All lessons <span>· 16 modules</span></button>{(["Beginner","Intermediate","Advanced"] as Level[]).map(l=><button key={l} className={level===l?"active":""} onClick={()=>setLevel(l)}>{l}<span>· {levels[l].sub}</span></button>)}</div><div className="curriculum-grid">{filtered.map(s=><button className={`subject-card ${selected.id===s.id?"selected":""}`} key={s.id} onClick={()=>select(s)}><div className="card-top"><span className={`pill ${levels[s.level].color}`}>{s.level} · {s.step}</span><span className="duration">{s.time}</span>{completed.includes(s.id)&&<span className="completed-mark" aria-label="Completed">✓</span>}</div><h3>{s.title}</h3><p>{s.description}</p><span className="card-arrow">→</span></button>)}</div></section>
  <section id="lesson-workspace" className="workspace"><div className="workspace-heading"><div><div className="eyebrow">LESSON {selected.step} · {selected.level.toUpperCase()}</div><h2>{selected.title}</h2></div><div className="session-count"><span className="dot"/> {selected.time} <span>·</span> repo-grounded</div></div><div className="lesson-tabs"><button onClick={()=>setTab("learn")} className={tab==="learn"?"active":""}>01 / Learn</button><button onClick={()=>setTab("lab")} className={tab==="lab"?"active":""}>02 / Lab</button><button onClick={()=>setTab("quiz")} className={tab==="quiz"?"active":""}>03 / Checkpoint</button></div><div className="workspace-body">
   {tab==="learn"&&<><div className="lesson-copy"><div className="eyebrow">OUTCOME</div><p className="outcome">{selected.outcome}</p><button className="complete-lesson" onClick={()=>setCompleted(completed.includes(selected.id)?completed.filter(id=>id!==selected.id):[...completed,selected.id])}>{completed.includes(selected.id)?"Completed ✓":"Mark lesson complete"}</button><div className="material-actions"><a className="button primary" href={base+guidePaths[selected.id]} target="_blank">Read lesson material ↗</a><a className="notebook-link inline-link" href={base+selected.notebook} target="_blank">Open companion notebook ↗</a></div><div className="eyebrow">THE IDEA</div><p className="big-copy">{selected.lesson}</p><div className="failure-strip"><span className="eyebrow">WATCH FOR</span>{selected.failures.map(f=><span key={f}>× {f}</span>)}</div></div><div className="diagram"><div className="diagram-label">THE STUDY LOOP</div><div className="flow"><div className="flow-box">Read<small>concept</small></div><i>→</i><div className="flow-box active-box">Run<small>notebook</small></div><i>→</i><div className="flow-box">Change<small>one thing</small></div></div><div className="diagram-note">Make the failure visible.<br/><span>Then turn the fix into a test.</span></div><div className="reference-list"><span className="eyebrow">SOURCE MATERIAL</span>{selected.refs.map((r,i)=><a key={r} href={r.startsWith("http")?r:base+r} target="_blank"><span>0{i+1}</span>{r} ↗</a>)}</div><div className="next-step"><span className="eyebrow">AFTER THIS LESSON</span><p>{selected.level==="Beginner"?"Continue to the next beginner module, then move on to the intermediate retrieval track.":selected.level==="Intermediate"?"Continue to the next retrieval experiment, keeping a golden set open as you tune.":"Continue to the next advanced pattern, carrying forward authorization, evaluation, and traceability."}</p><button onClick={()=>{const n=subjects[subjects.findIndex(x=>x.id===selected.id)+1];if(n)select(n)}}>Next lesson →</button></div></div></>}
   {tab==="lab"&&<div className="lab-layout workspace-single-column"><div className="lesson-copy"><div className="eyebrow">PRACTICAL LAB</div><p className="big-copy">{selected.exercise}</p><a className="notebook-link" href={base+selected.notebook} target="_blank">Run the guided notebook ↗</a><div className="lab-note"><b>Study ritual from the repo</b><br/>Use the notebook as the primary lab: read the concept cells, run the implementation, change one variable, run the tests, and explain one failure mode.</div></div><pre><code>{selected.code}</code></pre></div>}
   {tab==="quiz"&&<div className="quiz workspace-single-column"><div className="quiz-head"><div><div className="eyebrow">CHECKPOINT QUIZ</div><p>Answer from the lesson. The repo’s original quiz uses the same habit: explain why, then follow the source.</p></div><div className="score">{Object.keys(answers).length?`${score}/${selected.quiz.length}`:"—"}<small>score</small></div></div>{selected.quiz.map((q,i)=><div className="question" key={q.q}><b>{String(i+1).padStart(2,"0")} / {q.q}</b><div className="options">{q.options.map((o,j)=><button key={o} onClick={()=>setAnswers({...answers,[i]:j})} className={answers[i]===j?(j===q.answer?"correct":"wrong"):""}>{o}<span>{answers[i]===j?(j===q.answer?"✓":"×"):""}</span></button>)}</div></div>)}</div>}
  </div></section>
  <section id="how" className="footer-cta"><div className="eyebrow">THE REPO’S RULE OF THUMB</div><h2>Read the idea.<br/><span>Run the evidence.</span></h2><p>Every lesson here links back to the original <a href="https://github.com/mahsa-teimourikia/awesome-rag" target="_blank">awesome-rag</a> curriculum, notebooks, and references. Use the full quiz when you want one cross-topic knowledge check.</p><a className="button dark" href="/awesome-rag/quiz/">Open the full quiz ↗</a></section><footer><span>RAG / FIELD GUIDE</span><span>OPEN SOURCE · LEARN IN PUBLIC</span><a href="https://oneplusi.io" target="_blank">ONE+i · RESPONSIBLE AI ↗</a><a href="https://github.com/mahsa-teimourikia/awesome-rag" target="_blank">github ↗</a></footer>
 </main>;
}
