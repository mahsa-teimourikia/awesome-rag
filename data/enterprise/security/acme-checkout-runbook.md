# Acme checkout runbook

For an authenticated Acme support user, investigate checkout latency with the approved service-health dashboard and open an incident ticket when evidence supports it. Production restarts require incident-command approval.
